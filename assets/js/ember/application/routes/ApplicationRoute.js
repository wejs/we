
define(['we','ember'], function (we) {

  App.ApplicationRoute = Ember.Route.extend({
    actions: {
      // after change route
      // didTransition: function(transition) {
      //   window.scrollTo(0,0);
      // }

    }
});

});